# Orjanda Code Review — Phases 0–12 (commit `5319e83`)

Review date: 2026-08-12 · Working tree clean · Read-only review (no code or plan files modified)

---

## 1. Critical

1. ~~**JWT signing key is `[]byte(cfg.Server.Host)`** (`site.go:77-80`; default `"0.0.0.0"`; no secret field exists in `config.Config`). HS256 is symmetric and the key is public knowledge — anyone can forge an `admin@localhost` admin token. Roles are taken from token claims (stale until expiry) and revocation is an in-memory map (lost on restart, unbounded). This undermines the *entire* permission model: REST, RPC, WS, and CLI agent auth all trust this key.~~
   **Status: ✅ RESOLVED** (2026-08-12, uncommitted). **Validated:** confirmed that `site.go` signed HS256 tokens with `[]byte(cfg.Server.Host)` (default `"0.0.0.0"`) — a public, guessable key, so any attacker could forge an admin access token. **Fix:** added `config.AuthConfig.JWTSecret` (`auth.jwt_secret`, bound to `ORJANDA_AUTH_JWT_SECRET`); `config.ValidateJWTSecret` now requires a secret of ≥ `config.MinJWTSecretLength` (32) characters; `config.Load` fails fast on a missing/weak secret, and `site.NewSite` does the same for programmatic embeddings — the host-derived and hardcoded fallbacks were removed. Reference `orjanda.yaml` documents the new required key. The role-revocation part of the original finding (in-memory `revokedTokens`, no per-request role re-check) is out of scope for this fix and remains tracked as a follow-up.
   **Verification:** `TestSite_RejectsForgedTokens` proves tokens signed with `"0.0.0.0"` and the old hardcoded fallback return 401 while tokens from the site's own provider return 200; `TestNewSite_RequiresJWTSecret` proves missing/weak secrets are refused; config tests cover missing/weak/env-override secret handling (env override tested for both `auth.jwt_secret` and the reference YAML). All new tests pass under `go test -race` with an ambient `ORJANDA_AUTH_JWT_SECRET` set. `go build ./...`, `go vet ./...`, and `go test -count=1 ./...` all green.
2. ~~**Audit log is in-memory only.** `site.go:73` uses `audit.NewInMemoryLog()`; no DB-backed `audit.Log` implementation exists anywhere. TAD §13.1's "transactional audit write" guarantee (audit write failure rolls back the data write) is therefore not real in production — every write's audit record vanishes on restart.~~
   **Status: ✅ RESOLVED** (2026-08-12, uncommitted). **Validated:** `site.go:74` constructed `audit.NewInMemoryLog()` and no DB-backed `audit.Log` existed anywhere; the write-path guarantee of TAD §13.1 was structurally present (engines call `Write` inside their `dal.Tx` callback) but only an in-memory implementation was wired. **Fix:** added a DB-backed `audit.DBLog` (`audit/db.go`) storing entries in an `audit_entries` table (JSON-serialized `Changes`; per-dialect `timestamp`/`via_agent` storage). Because the TAD §13 `Log` interface carries no `dal.Tx`, the transactional path uses an additive `audit.TxWriter` interface (`WriteTx(ctx, tx, e)`), dispatched by `audit.WriteInTx` — the TAD `Log` interface is unchanged and `InMemoryLog` is untouched. Document Engine `Create`/`Update`/`Delete` (`document/engine.go`) and Workflow Engine `Execute` (`workflow/engine.go`) now call `audit.WriteInTx` inside their transaction, so a failed audit write aborts and rolls back the data write. `Site.InitAuditLog` (called from `Site.Compile`) creates the table and registers its docType mapping; the `orjanda/testing` harness wires the DB-backed log too. `Query` supports every `QueryFilter` field (equality filters, `ViaAgent`, `Since`, `Limit`, newest-first).
   **Verification:** `audit/db_test.go` proves round-trip fidelity (identity, agent metadata, `Changes` JSON), all filter dimensions, standalone `Write`, that a rolled-back transaction discards its audit entry, and — via a file-backed SQLite DB — that audit records **persist across a restart**. `document/engine_test.go` adds `TestEngine_AuditLog_DBBackedPersists` (real engine → DB log) and `TestEngine_AuditWriteFailure_RollsBackDataWrite` (audit failure rolls back the document write, the exact TAD §13.1 guarantee). `go build ./...`, `go vet ./...`, full `go test -count=1 ./...`, and `-race` on the touched packages (`.`, `audit`, `document`, `workflow`, `testing`) are all green; the `-tags integration` build compiles.

## 2. High

3. ~~**Agent session never carried in production paths.** `api/agent.go:81` and `cli/agent_chat.go:103` call `runtime.Execute` without `safety.WithSession`; `runtime.go:384-391` then creates a fresh session every turn → transcript/`seen`/`TargetCount` reset each turn → the TAD §11.1 discovery/operation split and §12.1 bulk-approval continuity **never work across turns**. `SessionManager` has no eviction/TTL (`session.go:102-108`) → unbounded memory growth.~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** confirmed both production paths called `runtime.Execute` with a context carrying no session id, so `sessionFor` (`runtime.go:384-391`) minted a fresh session per turn, and `SessionManager` (`agent/runtime/session.go`) had no expiry. **Fix:** the WS handler (`api/agent.go`) now creates one session per connection and carries it on every turn's context via `safety.WithSession`; `cli/agent_chat.go`'s `runAgentChat` does the same per invocation. `SessionManager` gained a TTL (`NewSessionManagerWithTTL`): `Session.lastAccess` is touched on every `Get`, expired sessions are evicted lazily on `New`/`Get` and via the exported `EvictExpired()`, keeping live memory within one TTL window of the creation rate. `runtime.Options.SessionTTL` selects the timeout (0 → `DefaultSessionTTL` = 30 min).
   **Verification:** `api/agent_ws_test.go` now drives two real WS turns through `AgentHandler.Stream` and asserts the turn-2 LLM request carries turn-1's full 6-message transcript (was 2 before the fix — confirmed by temporarily reverting). `cli/agent_chat_test.go` runs `runAgentChat` end-to-end against a fake `openai_compatible` server with piped stdin and asserts the turn-2 request carries the 4-message transcript (was 2 before the fix — also confirmed by temporary revert). `agent/runtime/session_test.go` covers TTL eviction, Get-touch lifetime extension, zero-TTL keep-alive, `EvictExpired` counting, opportunistic sweep on `New`, and a concurrent-access test. `go build ./...`, `go vet ./...`, full `go test -count=1 ./...`, `-race` on `agent/runtime`/`api`/`cli`, and the `-tags integration` build are all green.
4. ~~**Plan-mode transcript is malformed for real providers.** `plan.go:50-63` appends the assistant plan without `tool_calls`, then `plan.go:91` appends tool messages with empty `ToolCallID`. OpenAI/Anthropic reject tool messages without a matching `tool_call_id` (HTTP 400) — a real-provider session fails on the first plan turn. Only `MockLLM` tests pass; `synthesizeSummary` silently falls back to `deterministicSummary`.~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** `requestPlanWith` never appended the plan response to the transcript, and `runValidatedPlan` appended bare `role:"tool"` messages with empty `ToolCallID` — a transcript shape OpenAI/Anthropic reject (HTTP 400), so `synthesizeSummary`'s chat call failed and silently fell back to `deterministicSummary`. **Fix:** `requestPlanWith` now records the plan as an assistant content message (the plan is produced under a constrained ResponseFormat per TAD §11.3, so it stays plain content, not a tool call); each executed plan step is now appended as a well-formed pair — an assistant message declaring `tool_calls` with a generated id (`call_<ulid>`) followed by the `tool` message echoing that id as `ToolCallID`. Error paths (ref resolution / arg validation failures) get the same well-formed pair instead of an orphaned tool message. TAD §11.2 step b/c semantics (validate-before-execute, one correction turn, plan-level approval) are unchanged.
   **Verification:** new `TestExecutePlanModeWireFormat` drives a plan turn end-to-end through a real `OpenAIProvider` (stub `httptest` server) and asserts every captured request body is well-formed — each `tool` message carries a `tool_call_id` declared by a preceding assistant `tool_calls` entry, the plan request carries `response_format`, and the summary call records the plan as an assistant content message. Against the pre-fix code the test fails exactly as the finding predicted (`tool message with empty tool_call_id`, plan missing); it passes post-fix. Existing plan-mode tests (`TestExecutePlanMode`, `TestExecutePlanRejectedBeforeExecution`, `TestExecutePlanModeWithApproval`) all pass. `go build ./...`, `go vet ./...`, full `go test -count=1 ./...`, `-race` on `agent/runtime`, and the `-tags integration` build are all green.
5. ~~**Phase 9 codegen is not wired.** `ui.Regenerate`/`ui.ContentHash` are referenced only by `ui/ui_test.go` — never from `serve.go`, `site.go`, or `server.go`. `orjanda serve` never regenerates `@orjanda/codegen` output on registry hash change; the committed `orjanda-ui/src/generated/` output is stale or hand-verified at best. TAD §6.3 step 3 is structurally unmet.~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** `ui.Regenerate`/`ui.ContentHash` were referenced only by `ui/ui_test.go`; neither `cli/serve.go` nor `site.go` called them, and `Regenerate`'s default script path was CWD-relative (broken for `orjanda serve` run inside an Application directory). **Fix:** `ui/codegen.go` now ships `DefaultScriptPath()` (working-dir `orjanda-codegen.mjs` first, else the framework module's copy resolved from the source tree), `MarshalInput` (the exact canonical step-1 payload bytes), and `VerifyCommittedSchema` (node-free byte comparison of the on-disk payload). `cli/serve.go` wires both modes: `serveCodegenPass` (called after a successful `site.Compile()` when an `orjanda-ui` tree exists) runs `ui.Regenerate` — content-hash-gated so an unchanged Registry skips the node invocation (TAD §6.3 step 3), warn-and-continue on failure per serve's dev posture (TAD §16); `benchCodegen` fails fast when the committed `schema.json` no longer matches the compiled Registry — deliberately node-free because generated TypeScript is a build-time artifact embedded via `embed.FS` (PRD §17.4).
   **Verification:** `TestServeRegeneratesCodegenOnHashChange` drives `serveCodegen` through a real compiled site and asserts the first pass writes `schema.json`/`types.ts`/`documents.ts`/`.registry-hash`, an unchanged Registry is a no-op, and a Registry extended with a new Document re-triggers regeneration. `TestBenchFailFastOnStaleCodegen` proves bench accepts a fresh payload and rejects stale and missing ones. Commit-time gate: `TestCommittedCodegenOutputConsistent` byte-compares the checked-in `orjanda-ui/src/generated/schema.json` against `MarshalInput` without node and regenerates into a scratch dir to byte-compare `types.ts`/`documents.ts` when node is present — **it caught the committed output as stale** (base columns still `hidden:false`; the committed files were regenerated). `go build ./...`, `go vet ./...`, full `go test -count=1 ./...`, `-race` on `ui`/`cli`, `go build -tags integration`, and `tsc --noEmit` in `orjanda-ui` are all green.
6. ~~**App lifecycle hooks are dead code.** `app.Definition` is a plain struct (`app/app.go:9-17`); `site.go:99` and `cli/lifecycle.go:71-91` do `any(def).(app.Installable)`, which can never succeed for a struct value → `orjanda install` always prints *"declares no OnInstall hook"*. TAD §7 lifecycle is unreachable.~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** `cli/lifecycle.go:71-91` and `testing/site.go:133` asserted `any(def).(app.Installable)` on the `app.Definition` struct value, which can never implement the interface → `orjanda install` always printed *"declares no OnInstall hook"* and the harness could never run an Application's OnInstall. **Fix:** `app.Definition` now carries its TAD §7 "associated init type" in a new `Hooks any` field, resolved through `app.InstallHook()`/`UpgradeHook()`/`UninstallHook()`; `cli/lifecycle.go` and `testing/site.go` resolve via `def.InstallHook()`/`a.InstallHook()` (nil → the existing "declares no hook" message), so a Definition with none of the interfaces stays a valid no-op (TAD §7). The `Hooks` field extends the literal TAD §7 `Definition` struct — the TAD fixes `site.Install(app.Definition)`'s signature but is silent on how the init type is discovered, so the hook type must ride inside the value; flagged here rather than silently invented.
   **Verification:** `TestRunInstall_ExecutesOnInstallHook`/`TestRunUninstall_ExecutesOnUninstallHook` prove `orjanda install`/`uninstall` run the hooks (OnInstall once per invocation; OnUninstall receives `--drop-tables`); both fail against the pre-fix `any(def)` assertion ("OnInstall called 0 times" — confirmed by temporary revert). `TestWithApps_RunsOnInstallHook` proves the harness's PRD §11.3 path: an OnInstall that registers a Document yields a compiled, queryable doc. `app` tests cover hook resolution (`TestDefinition_LifecycleHookResolution`) and the Phase 1 ordering criterion (`TestResolveDAG_HookInstallOrderIsDependencyOrder`: a dependent's hook runs after its dependency's regardless of declaration order). `go build ./...`, `go vet ./...`, full `go test -count=1 ./...`, `-race` on `app`/`cli`/`testing`, and the `-tags integration` build are all green.
7. ~~**Bespoke permission gates bypass `perm.Engine`** (PRD §25.1 violation): `api/rpc/rpc.go:106-113` (`hasAnyRole`; the injected `permEngine` is dead at `:79`) and `workflow/engine.go:202-210` (engine's `permEngine` never referenced; empty `AllowedRoles` = zero checks).~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** `api/rpc/rpc.go` granted role-gated RPC methods with a hand-rolled `hasAnyRole` and never used the `permEngine` injected into `DispatchHandler`; `workflow/engine.go` did the same for transition `AllowedRoles` and never referenced its injected `permEngine`. **Fix:** added `perm.Engine.CheckRoles(ctx, docType, action, roles)` — the single role-gate authority for registry-less (synthetic) DocTypes. `rpc.DispatchHandler` now checks `"method:"+name` (TAD §9.2); workflow `Execute` and `AvailableTransitions` check the transition as the doc's own `docType` with action `"transition:"+action` (TAD §8.1 step 3); both are union-of-roles with the System Administrator override, `*` wildcard, empty-list = public, and TAD §13.3 `perm.denied` logging inside `CheckRoles`. The three bespoke `hasAnyRole` helpers (rpc, workflow, agent/tools) are deleted. The Agent Executor now re-checks method/custom tool `AllowedRoles` at execution through the same `CheckRoles` (the Runtime carries `opts.PermEngine`; it previously invoked method handlers with no role check at all), so a tool name called directly cannot bypass perm even when `ForIdentity` never advertised it.
   **Verification:** new `TestPermEngine_CheckRoles` (7 cases), `TestDispatch_MethodToolEnforcesRoles`, and `TestDispatch_CustomToolEnforcesRoles`; existing `TestRPC_MethodPermission`, `TestWorkflow_ExecuteRolePermissionCheck`, `TestWorkflow_AvailableTransitions`, `TestRPCMethodTools`, and `TestCustomToolsMergedAndRoleFiltered` now exercise the perm path (the RPC and workflow suites fail against the pre-fix bespoke gates only by construction, since behavior is preserved). `go build ./...`, `go vet ./...`, and full `go test -count=1 ./...` are green.
8. ~~**Meta API leaks gated field metadata.** `api/meta.go:94-110`: when the caller lacks write permission (or no fields are allowed), `AllowedFields` returns empty → `len(allowed) > 0` is false → **all** gated fields are returned *including their `permission` role string*. `GetDocMeta` also never gates on read permission.~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** `GetDocMeta` computed `can_read`/`can_write`/`can_create`/`can_delete` but served the full compiled field schema regardless; gated fields were filtered with `len(allowed) > 0 &&` — a map-emptiness heuristic that leaks **all** gated fields (role strings included) whenever `AllowedFields` returns empty, and `FieldMeta.Permission` echoed the raw `oj:"permission=role"` string to every caller. **Fix:** `GetDocMeta` now returns `403 Permission` before emitting any schema when the caller lacks read access to the DocType; the field filter is membership-based (`AllowedFields` determines which gated fields may appear, empty set ⇒ none appear) and only applies to fields with a `PermissionRole`; and `FieldMeta.Permission` is deleted — no Go or UI code consumed it (`orjanda-ui/src/types.ts` field also removed), so the API no longer discloses the role gate a field is behind.
   **Verification:** new `TestMetaAPI_GatedFieldsAndReadGate` drives `/api/v1/meta/ConfidentialNote` (field gated `oj:"permission=HR Manager"`) as three identities: a non-reader gets 403 with no schema; a reader lacking the role sees no gated field; a role-holder sees the gated field. All three cases assert no `permission` key appears in any field's JSON — and the 403 assertion fails against the pre-fix handler (which returned 200 with the full schema). `go build ./...`, `go vet ./...`, and `go test -count=1 ./...` are green.

*Resolved (not a bug):* the middleware's PUT/PATCH→`"write"` mapping is safe — `perm.Engine` normalizes `"update"`→`"write"` (`perm/engine.go:146-147,174`), matching the engine's `CheckAction(...,"write")`.

## 3. Medium / low

9. ~~**Migrator gaps** (`dal/migrator.go`): `Diff` is a hand-rolled table/column **set comparison**, not Atlas diffing (Atlas used only as a model representation). Consequences: `AlterColumns` is never populated → **column type changes are silently ignored**; **no `DropTables` detection** → orphaned tables never flagged and the `bench` fail-fast gate (`cli/serve.go:136` counts only CreateTables+AlterTables) misses them; `--allow-destructive` covers only `DROP COLUMN`. Also goose global-state misuse (`SetDialect`/`SetBaseFS`) races concurrent dialects; `Write` timestamp filenames collide if diffed twice in one second.~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** `Diff` compared columns by name only — `AlterColumns` was never populated, `DropTables` did not exist, the bench gate summed only CreateTables+AlterTables, the destructive gate checked only `DROP COLUMN`, goose's package-global `SetDialect`/`SetBaseFS` were unguarded, and `Write`'s second-per-second timestamp overwrote a prior file. **Fix:** `Diff` now detects type changes (live column type vs the dialect's `ColumnType` for the compiled field, compared via the new `Dialect.NormalizeColumnType`) and reports orphaned Orjanda-owned tables (id+deleted BaseDocument signature — `audit_entries`/unrelated tables excluded) as `SchemaDiff.DropTables` (sorted); `Write` gates dropped tables behind `--allow-destructive` alongside dropped columns, renders `DROP TABLE` (postgres renders `ALTER COLUMN … TYPE`, sqlite emits an explicit manual-rebuild `-- NOTE` rather than silently ignoring), dedupes same-second filenames (`_auto_2_` suffix), and `Up`/`Status` serialize goose's global state under `migrationMu`. The bench gate and `migrate diff`'s no-change check now count drops via `SchemaDiff.ChangeCount()`. TAD §14's `ColumnAlteration` lacked a column-name field (without it `ALTER COLUMN` cannot be generated) — added `ColumnName`, flagged as a TAD extension here rather than silently invented.
   **Verification:** live-SQLite tests — `TestMigratorDiff_DetectsOrphanedTable` (drop detected; unrelated/`audit_entries` tables not; no false-positive alters on a synced DB), `TestMigratorDiff_DetectsColumnTypeChange` (TEXT→INTEGER in `AlterColumns`), `TestMigratorWrite_DropTable_DestructiveGate` (errors without the flag naming the `DROP TABLE`, writes it with the flag), `TestMigrateUp_DropTable_LiveDB` (drop applied for real against an in-memory DB, table gone, second Up idempotent), `TestMigratorWrite_SameSecondDiffs_DoNotOverwrite`, `TestSchemaDiff_ChangeCountIncludesDrops`, plus dialect-level `AlterTable`/`NormalizeColumnType` cases for both dialects. `go build ./...`, `go vet ./...`, full `go test -count=1 ./...`, `-race` on `dal`/`cli`, and the `-tags integration` build are green.
10. ~~**`order_by` injection surface** (`api/rest/handlers.go:42` passes raw query → `resolveField` passes unknown names through → dialect quotes the column but appends the second token raw: `sqlite/dialect.go:120-128`, same in postgres). Multi-statement rejection by the drivers limits exploitability, but the direction token is unvalidated and ordering by hidden columns is possible.~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** `order_by` flowed unvalidated from the REST query string through `resolveField` (unknown names passed through) into the dialect's `ORDER BY`, where the second space-delimited token was appended raw; hidden user-data columns were sortable. **Fix:** `document.List` is now the single chokepoint — `validateOrderBy` (`document/engine.go`) allowlists the sort field against `compiled.Fields` (Go field name or DB column), rejects unknown fields, non-ASC/DESC directions, bare directions, and extra tokens with `VALIDATION_ERROR` (HTTP 400), and refuses hidden **user-data** fields; a new `schema.Field.System` flag (set on the framework-managed `getBaseDocumentFields`/`getBaseChildFields`) keeps hidden-but-system sort keys (`created_at`, `id`, …) sortable so trusted `DocMeta.SortField` defaults keep working. The REST handler already routes `order_by` through `engine.List`, and no agent/RPC path sets `OrderBy`. Defense-in-depth: both dialects now emit the direction token only when it is ASC/DESC (`isSortDirection`), dropping anything else before rendering. (`Field.System` is internal metadata the TAD is silent on — flagged here, not in the TAD.)
   **Verification:** `TestEngine_List_OrderByAllowlist` (11 cases: field/column names, ASC/DESC with first-row asserted, hidden system column ok, unknown field, unknown direction, extra tokens, bare direction, hidden user field/column rejected), `TestEngine_List_DefaultSortField` (trusted `SortField: created_at` default still resolves), `TestDialect_SelectSQL_OrderByDirectionHardening` (both dialects render `ORDER BY "name" DESC` for valid input and drop `DROP TABLE`/`salary`/`INJECTED` tokens across four injection strings), `TestREST_List_OrderByValidation` (HTTP surface: `order_by=title`/`title DESC`/`created_at` → 200 with correct ordering, `nosuchfield`/`title SENSITIVE`/`title DESC; DROP TABLE tasks; --` → 400 `VALIDATION_ERROR`). `go build ./...`, `go vet ./...`, full `go test -count=1 ./...`, and `-race` on `api`/`dal`/`document` are green.
11. ~~**Child-table naming is camelCase**, inconsistent with TAD §1.4: main tables use `camelToSnake+"s"` (`registry.go:128`) but child tables use `child.DocType + "s"` → `"LeaveDays"` (`migrator.go:95`, `dialect/sqlite/db.go:481`).~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** the child-table pluralization rule was duplicated ad hoc at eight sites (`child.DocType + "s"` in `migrator.go`, both dialect `RegisterDocs`/`CreateTables`, and three test helpers), each re-deriving the name instead of consuming a single computed value — any drift would silently split the create/query/migrate names (the finding's camelCase example reflected the raw-type-name variant that a drifted site would produce). **Fix:** `schema.CompiledChild.TableName` is now computed once at compile time (`parser.go`, `pluralizeTableName = camelToSnake + "s"`), and `pluralizeTableName` is the single shared derivation for **both** main tables (`registry.go`) and child tables; all eight consumer sites now read `child.TableName`. DocType stays singular snake (`child_row`); the table is plural (`child_rows`). The wire/codegen type `ChildTableJSON` intentionally exposes DocType only — table names never leave the server. **Verification:** `TestChildTableName_IsSnakeCasePlural` (live SQLite: `child_parents`/`child_rows` created, Go type name `ChildRow` absent from `ExistingTables`, compiled `TableName`/`DocType` asserted), `TestRegistry_CompileAndValidate` now asserts `child.TableName == "dummy_childs"`, and the existing child-table CRUD/query tests pass unchanged. `go build ./...`, `go vet ./...`, full `go test -count=1 ./...`, `-race` on `schema`/`dal`/`document`/`ui`/`cli`/`orjanda-core`, and the `-tags integration` build are green.
12. ~~**Bootstrap** (`orjanda-core/bootstrap.go:147`): logs the admin password via `slog.Info`; Query-then-Insert is not atomic → concurrent `serve` instances could create duplicate admins; the internal `db.Query` at `:69` runs inside a `tx` using `db` not `tx`.~~
   **Status: ✅ RESOLVED** (2026-08-14, uncommitted). **Validated:** bootstrap logged the plaintext admin password through `slog.Info`; the "any users?" check and the subsequent inserts were separate statements (the check ran inside the transaction but the inserts that followed were not all in it — two instances could both pass the check and both create an admin); one query inside the transaction used `db` instead of `tx`. A concurrent-writer reproduction (8 goroutines, one SQLite file) confirmed the atomicity gap. **Fix:** `Bootstrap` now runs the entire sequence — users-exists check, Role lookup/create, per-DocType permission grants, admin `User` + `UserRole` link — inside a **single `db.Transaction`**, so no interleaving instance can slip an admin in between the check and the inserts; the in-transaction query now uses `tx.Query`. The password is no longer logged: `bootstrap.go` records only the email via slog and returns the password to the caller, which prints it once to stdout (`cli/serve.go`, per TAD §4.2 step 3 — the operator still sees it exactly once, but it never reaches the structured log stream). Concurrency is defended in depth: `User.email`/`Role.role_name` uniqueness is now enforced by **both** dialects (the sqlite `CreateTable` was missing the `UNIQUE` emission PostgreSQL already had — `oj:"unique"` fields now get an inline `UNIQUE` constraint), and `isUniqueViolation` maps a raced duplicate insert (`SQLITE_CONSTRAINT_UNIQUE`/SQLSTATE `23505`) to the idempotent no-op instead of erroring. SQLite transactions are now `BEGIN IMMEDIATE` (`_txlock=immediate` DSN) with `busy_timeout=5000`, all applied **per-connection via the DSN** — the previous `PRAGMA …` `ExecContext` calls only configured the single pooled connection that ran them, so a deferred transaction that read-then-wrote under contention failed with SQLITE_BUSY on a stale WAL snapshot (the busy handler does not retry those). (`_txlock`/`busy_timeout` are modernc driver DSN keys, and the sqlite `UNIQUE` emission matches the existing postgres dialect — no TAD change needed.)
   **Verification:** `TestBootstrap_ConcurrentCallsCreateSingleAdmin` (8 concurrent bootstraps against one file-backed SQLite DB: no errors, exactly one admin user, one role, one permission row per DocType), `TestBootstrap_DoesNotLogPassword` (slog handler capture: buffer contains the admin email, never the generated password), `TestDB_UniqueConstraintPreventsDuplicateAdminEmail` (a direct DAL `Insert` of a second `admin@localhost` fails with `SQLITE_CONSTRAINT_UNIQUE` 2067 — proves the sqlite dialect now emits `UNIQUE` on the engine-bypassing DAL path bootstrap uses), alongside the existing hashed-password/role-grant/idempotency tests. Full `go build ./...`, `go vet ./...`, `go test -count=1 ./...`, `-race` on `orjanda-core`/`cli`/`dal`/`api`, and the `-tags integration` build are green.
13. **WS agent handler** (`api/agent.go`): `InsecureSkipVerify: true`, `context.WithoutCancel` (`:47`) so approval `ctx.Done` never fires, goroutine-per-message with no per-connection rate limit, per-turn session leak.
14. **Cache** (`cache/cache.go`): documented FIFO-with-re-append, not a true LRU (TAD §9.1 says LRU); `Set` is O(n) on every update.
15. **Event bus ordering**: handlers run in registration order; the "app-dependency order" claim is wiring responsibility, not enforced by `event.Bus`.

## 4. Confirmed solid

- **`perm.Engine`**: RBAC-first, ABAC rules AND-composed, `update`→`write` normalization — clean and matches TAD §2.4/§2.7.
- **`document.Engine`**: every CRUD path checks perm, dispatches hooks, builds audit entries, and drives workflow transitions; actions are consistent.
- **Registry compile**: snake_case pluralization, child-table assembly, `TableName` derivation.
- **Dialect SQL hygiene**: identifiers quoted with `%q` throughout, all values parameterized; the `order_by` direction token is now allowlisted at the engine and additionally gated to ASC/DESC at the dialect (finding 10); sqlite filter keys sorted for deterministic SQL; `oj:"unique"` fields emit `UNIQUE` in **both** dialects (finding 12), and the sqlite driver's pragmas (`WAL`, `foreign_keys`, `busy_timeout`, `_txlock=immediate`) are applied per-connection via the DSN so pooled connections cannot race with unconfigured locks.
- **Middleware chain order**: CORS → Auth → RateLimit → Permission.
- **`testing/site.go` harness**: `TestSite` embeds the real engines, no permission bypass — well-architected.
- **`agent/tools/tools.go`**: constant tool count per Document (TAD §10.1), gated fields filtered per identity via `perm.AllowedFields`.
- **Errors package**: six codes with wrapping; config loader via Viper.

## 5. Missing tests / validation gaps

- ~~No integration test of the real WS/CLI→Runtime **session flow**~~ (resolved 2026-08-14 — `TestAgentStreamWebSocket` asserts cross-turn transcript continuity over a real WS connection; `TestRunAgentChatCarriesSessionAcrossTurns` drives `runAgentChat` end-to-end).
- ~~No test with a real `llm.Provider` or a golden transcript asserting valid `tool_call_id` structure (plan-mode corruption is only visible by reading code).~~ (resolved 2026-08-14 — `TestExecutePlanModeWireFormat` drives a plan turn through a real `OpenAIProvider` and asserts every request's `tool_call_id`/`tool_calls` structure.)
- ~~No migrator tests against a live DB for type changes, dropped tables, or the destructive gate~~ (resolved 2026-08-14 — `TestMigratorDiff_DetectsColumnTypeChange`, `TestMigratorDiff_DetectsOrphanedTable`, `TestMigrateUp_DropTable_LiveDB`, `TestMigratorWrite_DropTable_DestructiveGate`, `TestMigratorWrite_SameSecondDiffs_DoNotOverwrite`).
- No auth test proving forged-token rejection (impossible — the secret is public).
- ~~No meta-API test for the field-filter leak case~~ (resolved 2026-08-14 — `TestMetaAPI_GatedFieldsAndReadGate` asserts non-reader 403, gated-field filtering for both non-holders and holders, and that no field JSON carries a `permission` role string).
- ~~No eviction/concurrency tests for `SessionManager`~~ (resolved 2026-08-14 — `TestSessionManager*` TTL/eviction/concurrency suite); ~~no test that codegen runs at serve time~~ (resolved 2026-08-14 — `TestServeRegeneratesCodegenOnHashChange`, `TestBenchFailFastOnStaleCodegen`, and the commit-time `TestCommittedCodegenOutputConsistent`); ~~no test that `OnInstall` is called (can't be — dead path)~~ (resolved 2026-08-14 — `TestRunInstall_ExecutesOnInstallHook`, `TestWithApps_RunsOnInstallHook`, `TestResolveDAG_HookInstallOrderIsDependencyOrder`).
- `go build`, `go vet`, `go test` all pass, but on unit coverage only; `.golangci.yml` exists but there's no evidence CI runs it.

## 6. Plan criteria marked complete but unsatisfied

| Phase | Criterion | Why unsatisfied |
|---|---|---|
| 9 | codegen runs at serve on registry change | ~~`Regenerate` never called~~ — resolved (2026-08-14): `serveCodegenPass` regenerates `orjanda-ui/src/generated/` on Registry content-hash change (TAD §6.3 step 3); `benchCodegen` is a fail-fast stale-client gate; commit-time consistency enforced by `TestCommittedCodegenOutputConsistent` |
| 1/4 | TAD §7 lifecycle hooks | ~~dead `Installable` assertion~~ — resolved (2026-08-14): `Definition.Hooks` resolved via `InstallHook`/`UpgradeHook`/`UninstallHook`; `orjanda install`/`uninstall` and the testing harness execute them; the dependency-order hook criterion is now genuinely tested |
| 8 | session continuity / approval continuity | ~~production paths never pass a session~~ — resolved (per-connection/per-invocation sessions via `safety.WithSession`, cross-turn transcript verified in WS and CLI integration tests) |
| 4/5 | transactional persistent audit | ~~only `InMemoryLog` exists~~ — resolved (DB-backed `audit.DBLog` wired transactionally in `site.go`) |
| 10 | `bench` fail-fast | ~~DropTables never detected~~ — resolved (2026-08-14): orphaned Orjanda tables are reported as `DropTables` and counted via `SchemaDiff.ChangeCount()` in the bench gate and `migrate diff` |

## 7. Recommended remediation order

1. ~~Fix the JWT secret (config/env field, reject weak defaults, role re-check per request or short expiry + DB-backed revocation).~~ ✅ **DONE (2026-08-12)** — secret is now a required config/env field (`auth.jwt_secret` / `ORJANDA_AUTH_JWT_SECRET`, min 32 chars), weak/missing secrets fail fast in both `config.Load` and `site.NewSite`; no derived/hardcoded fallback remains. Regression tests: `TestSite_RejectsForgedTokens`, `TestNewSite_RequiresJWTSecret`, and config secret tests. Follow-up (not done): per-request role re-check and DB-backed revocation.
2. ~~Implement a DB-backed `audit.Log` and wire it transactionally in `site.go`.~~ ✅ **DONE (2026-08-12)** — `audit.DBLog` backed by an `audit_entries` table; Document/Workflow Engines write through `audit.WriteInTx` inside their `dal.Tx`, so a failed audit write rolls back the data write (TAD §13.1); wired via `Site.InitAuditLog` from `Site.Compile` and the `orjanda/testing` harness. Regression tests: `TestDBLog_PersistsAcrossRestart`, `TestEngine_AuditWriteFailure_RollsBackDataWrite`, and the `TestDBLog_*`/`TestEngine_AuditLog_*` suites.
3. ~~Carry real sessions through the WS and CLI agent paths; add eviction/TTL to `SessionManager`.~~ ✅ **DONE (2026-08-14)** — per-connection session in `api/agent.go` and per-invocation session in `cli/agent_chat.go`, both threaded through `safety.WithSession`; `SessionManager` TTL/eviction (`NewSessionManagerWithTTL`, lazy `Get`/`New` eviction, `EvictExpired`, `runtime.Options.SessionTTL` defaulting to `DefaultSessionTTL` = 30 min). Regression tests: WS and CLI integration tests asserting cross-turn transcript continuity (both fail against the pre-fix code), plus the `TestSessionManager*` TTL/eviction/concurrency suite.
4. ~~Fix plan-mode transcript (real `tool_calls`, correct `ToolCallID`) and add a golden-transcript test.~~ ✅ **DONE (2026-08-14)** — plan recorded as an assistant content message; each plan step appended as assistant `tool_calls` + matching `tool` message with a generated `call_<ulid>` `ToolCallID`; regression test `TestExecutePlanModeWireFormat` round-trips a plan turn through a real `OpenAIProvider` and verifies well-formed `tool_call_id` structure on every captured request.
5. ~~Route RPC and workflow role checks through `perm.Engine`; delete the bespoke gates.~~ ✅ **DONE (2026-08-14)** — `perm.Engine.CheckRoles` is the single role-gate authority for synthetic DocTypes; RPC methods check `"method:"+name` (TAD §9.2), workflow transitions check via `docType`/`"transition:"+action` (TAD §8.1 step 3), the Agent Executor re-checks method/custom tools at execution, and the three bespoke `hasAnyRole` helpers are deleted. Regression tests: `TestPermEngine_CheckRoles`, `TestDispatch_MethodToolEnforcesRoles`, `TestDispatch_CustomToolEnforcesRoles`, plus the existing RPC/workflow/tool-visibility suites passing through the perm path.
6. ~~Wire `ui.Regenerate` into serve/bench and add a commit-time generated-output consistency check.~~ ✅ **DONE (2026-08-14)** — `serveCodegenPass` (hash-gated dev regeneration) and `benchCodegen` (node-free fail-fast stale-client gate) wired in `cli/serve.go`; `ui.DefaultScriptPath` makes the node script resolvable outside the framework repo root. Regression tests: `TestServeRegeneratesCodegenOnHashChange`, `TestBenchFailFastOnStaleCodegen`, and the commit-time `TestCommittedCodegenOutputConsistent` — the last caught the previously-stale committed `orjanda-ui/src/generated/` output, which was regenerated.
7. ~~Give `app.Definition` real lifecycle methods (or a resolvable OnInstall registry), or downgrade the Phase 1/4 claims.~~ ✅ **DONE (2026-08-14)** — `Definition.Hooks` init type resolved via `InstallHook`/`UpgradeHook`/`UninstallHook`; `orjanda install`/`uninstall` and the `testing` harness execute them. Regression tests: `TestRunInstall_ExecutesOnInstallHook`, `TestRunUninstall_ExecutesOnUninstallHook`, `TestWithApps_RunsOnInstallHook`, `TestDefinition_LifecycleHookResolution`, `TestResolveDAG_HookInstallOrderIsDependencyOrder`.
8. ~~Add migrator `DropTables` + type-change (`AlterColumns`) detection, include drops in the bench gate, add migration tests.~~ ✅ **DONE (2026-08-14)** — `Diff` detects type changes (`AlterColumns`) and orphaned Orjanda-owned tables (`DropTables`); `Write` gates dropped tables behind `--allow-destructive`, renders `DROP TABLE` / `ALTER COLUMN TYPE` (sqlite surfaces a manual-rebuild note), and dedupes same-second filenames; `Up`/`Status` serialize goose's global state; the bench gate and `migrate diff` count drops via `SchemaDiff.ChangeCount()`. Regression tests: `TestMigratorDiff_DetectsColumnTypeChange`, `TestMigratorDiff_DetectsOrphanedTable`, `TestMigratorWrite_DropTable_DestructiveGate`, `TestMigrateUp_DropTable_LiveDB`, `TestMigratorWrite_SameSecondDiffs_DoNotOverwrite`, `TestSchemaDiff_ChangeCountIncludesDrops`, plus dialect-level `AlterTable`/`NormalizeColumnType` tests. (`ColumnAlteration.ColumnName` added — flagged as a TAD §14 extension, see finding 9.)
9. Allowlist/validate `order_by` and direction at the API layer; snake_case child tables; ~~fix the meta field-filter leak~~ ✅ (2026-08-14 — `GetDocMeta` read-gates to 403, filters gated fields by membership in `AllowedFields` rather than map-emptiness, and no longer echoes the field's required role string; `TestMetaAPI_GatedFieldsAndReadGate`). ✅ (2026-08-14 — `order_by` is allowlisted at the engine chokepoint via `validateOrderBy` in `document/engine.go`: unknown fields, non-ASC/DESC directions, bare directions, extra tokens, and hidden user-data columns → 400 `VALIDATION_ERROR`; both dialects additionally drop non-ASC/DESC direction tokens as defense-in-depth; covered by `TestEngine_List_OrderByAllowlist`, `TestDialect_SelectSQL_OrderByDirectionHardening`, and `TestREST_List_OrderByValidation`.) ✅ (2026-08-14 — child-table names are now pluralized snake_case derived once at compile time: `CompiledChild.TableName` (`parser.go`) via the shared `pluralizeTableName` helper also used for main tables, with all eight consumer sites reading `child.TableName`; `TestChildTableName_IsSnakeCasePlural` asserts the live SQLite table is `child_rows`, not the Go type name. See finding 11.)
10. Re-run lint/vet/tests; update plan markers with code evidence rather than rewording criteria.
