// Package schema contains the Document contract, BaseDocument/BaseChild embed
// types, the CompiledDoc/Field metadata types, the oj struct-tag parser, and
// the Registry that stores every registered Document after compilation.
//
// See TAD §2.1–§2.2 and PRD §10 for the full specification.
// Implemented in Phase 1.
package schema
